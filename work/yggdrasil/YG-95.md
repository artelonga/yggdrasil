---
id: 95
title: "Commit + rastrear Phase 0 de notas (NoteStore + wikilinks + backlinks + graph + editor)"
type: user-story
status: done
priority: critical
conventional_commit: "feat(notes):"
semver_bump: minor
labels:
  - type:feat
  - module:content
module: yggdrasil-web
parent: 94
created_at: 2026-06-06T00:00:00Z
updated_at: 2026-06-06T00:00:00Z
---

## As
O time, que tem o sistema de notas (Phase 0) **em produção mas não commitado nem rastreado**.

## I Need
Commitar o código da Phase 0 com bump de versão + CHANGELOG, e registrar retroativamente o que shipou. **Gate da release** — não dá pra lançar conteúdo com código fora do controle de versão.

## So That
As notas entram numa release nomeada (v2.2.0) e o histórico fica auditável.

## Context
Phase 0 implementada inline (sem YG task) e em prod desde 2026-06-06 (`yggdrasil-artelonga.fly.dev`):
- `yggdrasil-core/src/instance/note.rs` — `NoteStore` (escrita atômica) + parser de wikilinks (porte de `co/core/src/wikilink.rs`) + backlinks/graph.
- `yggdrasil-web/src/api/instances.rs` — 4 rotas `/api/v1/instances/{id}/notes[/{slug}]` (PUT/GET/DELETE + list com backlinks/graph).
- `static/universos/instance.{js,html}` — markdown render + wikilink edges + editor de nota + sidebar Notas.
- `SCHEMA_VERSION` 1→2 (aditivo, migração forward no-op).

## Acceptance
- **GIVEN** o working tree com a Phase 0
  **WHEN** commitar
  **THEN** branch `feat/YG-95-notes-phase0`, commit `feat(notes): …`, bump de versão (2.1.x → 2.2.0), `CHANGELOG.md` `[Unreleased]` descrevendo notas + wikilinks + graph.
- **GIVEN** a suíte
  **WHEN** `cargo fmt --check && cargo clippy -D warnings && cargo test`
  **THEN** os três passam (os testes da Phase 0 já existem: NoteStore, wikilink, backlinks, v1→v2 round-trip, rotas REST).
- **GIVEN** o board
  **WHEN** lê YG-95
  **THEN** documenta retroativamente o que a Phase 0 entregou (esta tarefa é o registro).

## Dependencies
- Nenhuma (Phase 0 ✓ em disco). É pré-requisito de release para todo o Tema A.
