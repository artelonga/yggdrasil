---
id: 39
title: "Promote YggGame to a real adapter trait used by all four games"
type: user-story
status: todo
priority: medium
conventional_commit: "refactor(games)!:"
semver_bump: patch
labels:
  - type:refactor
  - module:games
module: yggdrasil
parent: 48
created_at: 2026-05-18T00:00:00Z
updated_at: 2026-05-18T00:00:00Z
---

## As

A maintainer of yggdrasil

## I Need

`YggGame` trait moved to its own module and implemented by snake, tetris, invaders, and poker; route boilerplate collapsed into `make_session_router::<G>()`

## So That

Each new game becomes a trait impl plus 30 LOC of route, not a full route module duplicate.

## Context

- **Principles:** §1 (composition), §5 (segregated state)
- **Scope:** `YggGame` is declared in `yggdrasil-core/src/games/snake.rs` and re-exported, but only `YggSnake` implements it. `YggTetris`, `YggInvaders`, `YggPoker` each expose ad-hoc `tick`/`render_json`/`score` shapes, and the route layer special-cases each. Move `YggGame` to its own module (`yggdrasil-core/src/games/adapter.rs`), implement it for all four, then generalise `snake_routes`/`tetris_routes`/`invaders_routes` into a single `make_session_router::<G: YggGame>(…)`.

## Acceptance

- One `pub trait YggGame` in `yggdrasil-core::games::adapter`.
- 4 impls (`YggSnake`, `YggTetris`, `YggInvaders`, `YggPoker` or its single-player sub-component).
- `make_session_router::<G>()` exists; `snake_routes`, `tetris_routes`, `invaders_routes` collapse to ~30 LOC each (state struct + boot).
- No behaviour change visible to clients (same JSON shapes, same routes).

## Blast radius

Single-player game routes only. Poker untouched unless we want it. Backwards-compatible at the HTTP layer.
