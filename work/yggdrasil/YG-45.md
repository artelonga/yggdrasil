---
id: 45
title: "Trim auth.rs and api/me.rs (each >600 LOC)"
type: user-story
status: done
priority: low
conventional_commit: "refactor(auth):"
semver_bump: patch
labels:
  - type:refactor
  - module:auth
module: yggdrasil
parent: 49
created_at: 2026-05-18T00:00:00Z
updated_at: 2026-05-18T00:00:00Z
---

## As

A maintainer of yggdrasil

## I Need

`auth.rs` split into `auth/{jwt, magic_link, state}.rs`; tests moved to sibling test files

## So That

No file exceeds 300 LOC; sign/verify and magic-link request/verify each live in one place.

## Context

- **Principles:** §2 (SRP)
- **Scope:** Both files mix domain logic, HTTP handlers, and large `#[cfg(test)]` blocks. Move tests into sibling `tests/auth.rs` and `tests/me.rs` (integration test crates) or `_tests.rs` modules. Inside `auth.rs`, split: `auth/jwt.rs` (sign/verify), `auth/magic_link.rs` (request/verify code), `auth/state.rs` (`AuthState`, DB schema).

## Acceptance

- No single file > 300 LOC after split (target, not strict).
- All tests still pass.

## Blast radius

Imports only; no public API change.
