[package]
name = "universe-poker"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
universe-sdk = { path = "../universe-sdk" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
