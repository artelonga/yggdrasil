[package]
name = "echo"
version = "0.1.0"
edition = "2021"
publish = false
description = "Universo echo mínimo — smoke-test de build tooling"

[lib]
crate-type = ["cdylib"]

[dependencies]
universe-sdk = { path = "../universe-sdk" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
