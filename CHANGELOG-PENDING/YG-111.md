## YG-111 — Superfície de exploração do corpus (Ayvu Rapyta)

- Commita a superfície de corpus antes só-local: `public::corpus_json` (leitor do JSON
  baked `corpus/<slug>.json` do repo `comunicacao`), a rota `GET /api/v1/comunicacao/corpus/{slug}`
  + `/universos/corpus`, e o cliente `corpus.html`/`corpus.js` (exploração: capítulos →
  versos Mbyá ⟷ Español, glosas/partículas/NOTAS, pan/zoom/inspector/Caderno local-first).
- Plane de língua `es → spanish` no `LexiconStore::lang_dir`.
- Camada arquitetural: ver `docs/adr/domain-layering-co-yggdrasil-comunicacao.md` — o corpus é
  **forma do universo comunicação**, hospedada pela plataforma yggdrasil.
- Pendências de GA (follow-up): testes de rota do endpoint corpus, catálogo `languages/es`,
  seed de `corpus/` no Dockerfile.
